Obviously 0 is lower bound of $`A`$. Now let's say there exists $`x`$ in $`A`$ other than 0. It is a contradiction since $`x \notin A_n`$ where
```math
n = \frac{1}{\lfloor x \rfloor}
```
Therefore $`A = \{0\}`$